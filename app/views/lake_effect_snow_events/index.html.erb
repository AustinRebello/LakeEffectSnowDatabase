<h1 class="text-center">Lake Effect Snow Events</h1>



<div class = "p-1 text-center">
  <%= link_to "Simple Search Events", search_lake_effect_snow_events_path, class:"btn btn-dark" %>
  <%= link_to "Advanced Search Events", advancedSearch_lake_effect_snow_events_path, class:"btn btn-dark" %>
  <%= link_to "New Event", new_lake_effect_snow_event_path, class:"btn btn-dark" %>
</div>

<div id="lake_effect_snow_events">
  <table id="sortTable" class="table table-striped table-hover table-bordered">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Event Name</th>
      <th scope="col">Start Time</th>
      <th scope="col">End Time</th>
      <th scope="col">Start Peak Time</th>
      <th scope="col">End Peak Time</th>
      <th scope="col">Avg Lake Surface Temperature</th>
      <th scope="col">LES Event Type</th>
    </tr>
  </thead>
  <tbody class="table-group-divider">
   <% @lake_effect_snow_events.each do |event| %>
    <tr>
      <td><%=link_to event.eventName, event %></td>
      <td><%= event.startDate %> at <%= event.startTime %>Z</td>
      <td><%= event.endDate %> at <%= event.endTime %>Z</td>
      <td><%= event.peakStartDate %> at <%= event.peakStartTime %>Z</td>
      <td><%= event.peakEndDate %> at <%= event.peakEndTime %>Z</td>
      <td><%= event.averageLakeSurfaceTemperature %></td>
      <td><%= event.eventType %></td>
    </tr>
  <% end %>
  </tbody>
    
</table>
  
</div>


<script>
  $(document).ready(function () {
    if(document.getElementById('sortTable_wrapper')==null){
      $('#sortTable').dataTable({
        paging: false,
        searching: false,
      });
      $('.dataTables_length').addClass('bs-select');
    }
  });
</script>